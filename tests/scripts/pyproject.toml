[project]
name = "mavlink-test-scripts"
version = "0.1.0"
description = "Python test scripts for MAVLink server"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "pymavlink>=2.4.0",
]
